server {
    listen 80;
    server_name writing.iil.pet;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name writing.iil.pet;

    ssl_certificate /etc/letsencrypt/live/writing.iil.pet/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/writing.iil.pet/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    client_max_body_size 100M;

    location /static/ {
        alias /opt/writing-hub/staticfiles/;
        expires 30d;
    }

    location /media/ {
        alias /opt/writing-hub/media/;
    }

    location / {
        proxy_pass http://127.0.0.1:8097;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }
}
