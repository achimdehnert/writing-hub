name: _disabled_ci
on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'This workflow is disabled — use ci-cd.yml instead'
        required: false
jobs:
  noop:
    runs-on: ubuntu-latest
    if: false
    steps:
      - run: echo "disabled"
